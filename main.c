#include <stdio.h>
#include <stdlib.h>
#include "teste.h"

int main() {
	FILE *fileStudent;
	FILE *fileUniversity;

	fileStudent = fopen("/inStudent", "r");
	fileUniversity = fopen("/inUniversity", "r");

	if
	makeTeste();

	fclose(fileStudent);
	fclose(fileUniversity);

	return 0;
}